<?xml version="1.0" encoding="utf-8"?>
<manifest package="example.com.explicitreceiver"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="abc.def.PERMISSION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="example.com.explicitreceiver.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<receiver-->
            <!--android:name="example.com.explicitreceiver.ExplicitReceiver"-->
            <!--android:enabled="true"-->
            <!--android:exported="true"-->
            <!--android:permission="abc.def.PERMISSION">-->
            <!--<intent-filter>-->
                <!--<action android:name="net.text.dev" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->
    </application>

</manifest>